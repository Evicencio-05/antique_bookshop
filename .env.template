# Copy this file to .env and adjust values as needed.
# Windows PowerShell:  just env-copy
# Git Bash:            cp .env.template .env

# Core Django settings
DEBUG=True
SECRET_KEY=replace-with-a-secure-random-string
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
# SQLite for local development (default)
DATABASE_URL=sqlite:///db.sqlite3

# For PostgreSQL, comment out the SQLite line above and uncomment below, then fill values:
# DATABASE_URL=postgres://user:password@host:5432/dbname

# Security / CSRF (optional; set if using custom domains)
# CSRF_TRUSTED_ORIGINS=http://localhost:8000

# Email (optional; for development)
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# CI-only secret (do not reuse SECRET_KEY)
# Used by GitHub Actions workflow
DJANGO_SECRET_KEY=replace-with-a-different-secret-for-ci
