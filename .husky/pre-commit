# Husky pre-commit hook (bash/sh) to run Python pre-commit via uv
# Works with Git Bash on Windows and POSIX shells on macOS/Linux.
# If uv is not available, falls back to a system pre-commit binary.

uv run pre-commit run --hook-stage pre-commit
